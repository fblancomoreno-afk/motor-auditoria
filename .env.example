# Copia este archivo como .env y rellena tu API key real
# La key NUNCA se expone al navegador — solo vive en el servidor

ANTHROPIC_API_KEY=sk-ant-api03-...

# Puerto del servidor (opcional, por defecto 3000)
PORT=3000
